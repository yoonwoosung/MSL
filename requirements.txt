# 실제 코드(main.py + modules/)가 사용하는 패키지만 명시했습니다.
# 아래는 버전 하한 가이드이며, 최종 제출 시에는 반드시 가상환경에서
#   pip freeze > requirements.txt
# 로 본인 환경의 정확한 버전을 다시 생성해 제출하세요(재현성 보장).

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3
