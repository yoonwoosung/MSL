업로드한 캡처 가이드 내용을 기준으로, **제목은 크게 / 핵심은 굵게 / 복붙하기 쉽게** 다시 정리했어. 
아래 내용을 그대로 복사해서 **PPT, 노션, 워드, Claude**에 붙여넣으면 보기 좋게 쓸 수 있어.

# 📌 머신러닝 팀 프로젝트 발표 캡처 가이드

## DDoS 탐지 모델 발표용 코드·결과물 캡처 정리본

---

# 1. 슬라이드별 캡처 구성

---

## 슬라이드 1. 표지

### 캡처 없음

PPT에 아래 내용을 직접 작성하면 됩니다.

* 프로젝트 주제
* 팀명
* 팀원 이름
* 과목명

### 선택 캡처

* `result/confusion_matrix.png`

### 넣는 이유

발표 주제를 시각적으로 보여주고 싶다면 혼동행렬 이미지를 배경 썸네일처럼 작게 넣어도 좋습니다.

---

# 슬라이드 2. 문제 제기

## 왜 머신러닝으로 DDoS를 탐지하는가?

---

## 캡처 ① 파이프라인 진입점 docstring

### 종류

📝 코드 캡처

### 정확한 위치

`main.py` 1~9줄
상단 docstring + 흐름 주석

### 캡처 방법

VS Code에서 `main.py`를 열고 최상단 부분을 캡처합니다.

### 들어갈 이유

이 프로젝트가 어떤 흐름으로 문제를 해결하는지 한눈에 보여줄 수 있습니다.

### 추천 표시

아래 흐름 키워드에 강조색 밑줄을 넣습니다.

**전처리 → 비교 → 학습 → 평가**

---

# 슬라이드 3. 데이터 소개

## 85만 행, 이진 분류, 거의 균형 데이터

---

## 캡처 ① 데이터 크기·클래스 분포

### 종류

💻 터미널 출력 캡처

### 정확한 위치

VS Code 터미널

### 실행 명령어

```bash
python -c "import pandas as pd; df=pd.read_csv('data/ddos_dataset.csv'); print('shape:', df.shape); print(df['target'].value_counts())"
```

### 들어갈 이유

데이터가 약 85만 행 규모이며, 정상/공격 비율이 약 53:47로 거의 균형임을 보여주는 근거입니다.

### 추천 표시

* `shape` 값에 박스 표시
* 클래스 개수 두 줄에 박스 표시
* 옆에 **“거의 균형 데이터”** 주석 추가

---

## 캡처 ② 클래스 분포 막대그래프

### 종류

🖼️ 결과 이미지 또는 PPT 차트

### 만드는 방법

`df['target'].value_counts().plot(kind='bar')`로 막대그래프를 생성하거나,
위 터미널 수치를 PPT 차트로 직접 만들면 됩니다.

### 들어갈 이유

정상과 공격 데이터의 비율이 비슷하다는 점을 시각적으로 보여줄 수 있습니다.

### 추천 표시

두 막대 높이가 비슷하다는 점을 화살표로 강조합니다.

---

# 슬라이드 4. 전공 적합도

## 왜 정보통신공학 프로젝트인가?

---

## 캡처 ① ID_COLUMNS 코드

### 종류

📝 코드 캡처

### 정확한 위치

`modules/preprocessing.py` 27~35줄
`ID_COLUMNS`

### 들어갈 이유

`Source IP`, `Dest IP`, `Source Port`, `Dest Port`, `Flow ID`, `Timestamp`처럼
네트워크 도메인 용어가 코드에 직접 등장합니다.

즉, 이 프로젝트가 단순 분류 문제가 아니라 **정보통신 데이터 기반 문제**임을 보여줄 수 있습니다.

### 추천 표시

아래 단어에 색 강조를 넣습니다.

* `Source IP`
* `Dest IP`
* `Source Port`
* `Dest Port`
* `Flow ID`
* `Timestamp`

---

# 슬라이드 5. EDA

## 컬럼, 결측치, 분포 확인

---

## 캡처 ① 컬럼 목록 + 결측/분포 요약

### 종류

💻 터미널 출력 캡처

### 실행 명령어

```bash
python -c "import pandas as pd; df=pd.read_csv('data/ddos_dataset.csv'); print('컬럼:', df.columns.tolist()); print('결측:', df.isna().sum().sum()); print(df.describe().T[['mean','std','min','max']])"
```

### 들어갈 이유

데이터에 어떤 컬럼이 있는지, 결측치가 있는지, 스케일 차이가 있는지 확인할 수 있습니다.

이 결과를 통해 전처리가 왜 필요한지 설명할 수 있습니다.

### 추천 표시

식별자 컬럼에 빨간 박스를 넣습니다.

예시:

* `Source IP`
* `Dest IP`
* `Source Port`
* `Dest Port`

이 컬럼들은 뒤에서 제거할 대상이라는 점을 미리 보여줄 수 있습니다.

---

## 캡처 ② 전처리 실행 로그

### 종류

💻 터미널 출력 캡처

### 실행 방법

```bash
python main.py
```

실행 후 아래 부분을 캡처합니다.

```text
학습에 사용하는 특성 수: 4개
전처리 완료: 학습 (682068, 4), 테스트 (170517, 4)
```

### 들어갈 이유

최종적으로 4개 특성만 사용했다는 점을 보여줄 수 있습니다.

이는 **차원의 저주가 크지 않으며 PCA가 반드시 필요하지 않은 구조**라는 설명과 연결할 수 있습니다.

### 추천 표시

`4개`에 강조색을 넣습니다.

---

# 슬라이드 6. 전처리

## 식별자 제거 = 데이터 누수 차단

---

# ⭐ 핵심 슬라이드

---

## 캡처 ① 식별자 제거 함수

### 종류

📝 코드 캡처

### 정확한 위치

`modules/preprocessing.py` 38~48줄
`_drop_identifier_columns()`

추가로 함께 보여주면 좋은 위치:

`modules/preprocessing.py` 27~35줄
`ID_COLUMNS`

### 들어갈 이유

이 발표의 핵심입니다.

IP, Port 같은 식별자 컬럼을 제거함으로써 모델이 공격 패턴을 학습하는 것이 아니라
특정 주소나 포트를 암기하는 문제를 막았다는 점을 보여줄 수 있습니다.

### 추천 표시

아래 부분에 굵은 박스를 넣습니다.

* `ID_COLUMNS`
* `to_drop`
* `_drop_identifier_columns()`

---

## 캡처 ② 스케일러 누수 방지

### 종류

📝 코드 캡처

### 정확한 위치

`modules/preprocessing.py` 108~111줄

### 핵심 코드

```python
fit_transform(X_train)
transform(X_test)
```

### 들어갈 이유

테스트 데이터의 정보가 학습 과정에 섞이지 않도록 처리했다는 점을 보여줄 수 있습니다.

즉, 전처리 단계에서도 데이터 누수를 막았다는 증거입니다.

### 추천 표시

`fit_transform`과 `transform` 차이에 대비 화살표를 넣습니다.

---

## 캡처 ③ 누수 방지 실행 로그

### 종류

💻 터미널 출력 캡처

### 실행 방법

```bash
python main.py
```

### 캡처할 로그

```text
[누수 방지] 식별자 컬럼 제거: ['Source IP', 'Dest IP', 'Source Port', 'Dest Port']
[범주형 인코딩] LabelEncoder 적용: ['Highest Layer', 'Transport Layer']
```

### 들어갈 이유

식별자 컬럼이 실제 실행 과정에서 제거되었다는 증거입니다.

### 추천 표시

제거된 컬럼 리스트에 빨간 박스를 넣습니다.

---

# 슬라이드 7. 모델 후보

## Logistic Regression, Decision Tree, Random Forest

---

## 캡처 ① 모델 후보 정의

### 종류

📝 코드 캡처

### 정확한 위치

`modules/evaluate.py` 39~55줄
`_build_candidates()`

### 들어갈 이유

총 3개 모델을 비교했다는 점을 코드로 보여줄 수 있습니다.

각 모델의 이론적 의미도 발표에서 연결할 수 있습니다.

### 발표 연결

* Logistic Regression
  → Lecture 8, 시그모이드와 로그 손실

* Decision Tree
  → Lecture 10, 정보 이득과 분할

* Random Forest
  → Lecture 10, 배깅과 앙상블

### 추천 표시

각 모델명에 색 강조를 넣습니다.

* `LogisticRegression`
* `DecisionTree`
* `RandomForest`

---

# 슬라이드 8. 코드 실행 흐름

## main.py가 전체 실험을 제어한다

---

## 캡처 ① main() 실행 순서

### 종류

📝 코드 캡처

### 정확한 위치

`main.py` 26줄부터 끝까지
`def main()` 본문

### 들어갈 이유

전체 코드가 어떤 순서로 실행되는지 발표자가 설명할 수 있습니다.

### 추천 표시

각 단계마다 번호 배지를 넣습니다.

① 전처리
② 모델 비교
③ 학습/튜닝
④ 평가
⑤ 저장

---

## 캡처 ② 설정 변수

### 종류

📝 코드 캡처

### 정확한 위치

`main.py` 16~23줄

### 핵심 변수

* `DATA_FILE`
* `TARGET_COL`
* `MODEL_SAVE_PATH`
* `RESULT_DIR`
* `RUN_TUNING`

### 들어갈 이유

데이터 경로, 타깃 컬럼, 최종 모델 저장 위치, 결과 폴더, 튜닝 여부를 한눈에 보여줄 수 있습니다.

### 추천 표시

아래 항목에 박스를 넣습니다.

* `final_model.pkl`
* `RUN_TUNING`

---

# 슬라이드 9. 모델 비교 결과

## 같은 조건에서 공정하게 비교했다

---

## 캡처 ① 모델별 train/test 로그

### 종류

💻 터미널 출력 캡처

### 실행 방법

```bash
python main.py
```

### 캡처할 로그 예시

```text
[LogisticRegression] train=0.9895 / test=0.9895 (gap=-0.0000)
[DecisionTree]       train=0.9984 / test=0.9980 (gap=0.0004)
[RandomForest]       train=0.9984 / test=0.9979 (gap=0.0006)
```

### 들어갈 이유

모델을 같은 조건에서 비교했다는 근거입니다.

또한 train/test gap이 작아 과적합이 크지 않다는 점을 설명할 수 있습니다.

### 추천 표시

* `RandomForest` 줄에 박스 표시
* `gap` 값에 강조 표시

---

## 캡처 ② 비교표 파일

### 종류

🖼️ 결과 파일 또는 터미널 출력

### 정확한 위치

`result/model_comparison.csv`

### 실행 명령어

```bash
python -c "import pandas as pd; print(pd.read_csv('result/model_comparison.csv').to_string(index=False))"
```

### 들어갈 이유

Accuracy뿐만 아니라 Precision, Recall, F1-score까지 포함한 정식 비교표를 보여줄 수 있습니다.

### 추천 표시

`f1` 열을 강조합니다.

---

# 슬라이드 10. 최종 모델 선택

## 왜 Random Forest인가?

---

## 캡처 ① RF 선택 이유 docstring

### 종류

📝 코드 캡처

### 정확한 위치

`modules/train.py` 4~13줄

### 들어갈 이유

Random Forest를 선택한 이유가 코드 문서에 정리되어 있습니다.

발표에서 “성능이 좋아서”가 아니라 다음 기준으로 선택했다고 말할 수 있습니다.

* 비선형 패턴 대응
* 스케일에 상대적으로 둔감
* 배깅 기반 안정성
* Feature Importance를 통한 설명 가능성

### 추천 표시

아래 키워드에 강조 표시합니다.

* `bagging`
* `feature_importances_`
* `RandomForest`

---

## 캡처 ② 학습·저장 함수

### 종류

📝 코드 캡처

### 정확한 위치

`modules/train.py` 22~38줄

### 들어갈 이유

최종 모델이 실제로 학습되고, `final_model.pkl`로 저장된다는 점을 보여줄 수 있습니다.

### 추천 표시

아래 코드에 박스를 넣습니다.

* `RandomForestClassifier`
* `save_model`
* `final_model.pkl`

---

# 슬라이드 11. 성능 평가

## Test F1 0.9979, Overfit Gap 0.0005

---

# ⭐ 핵심 슬라이드

---

## 캡처 ① metrics.txt 전체

### 종류

🖼️ 결과 파일 캡처

### 정확한 위치

`result/metrics.txt`

### 캡처 방법

VS Code에서 파일을 열거나 아래 명령어를 실행합니다.

```bash
type result\metrics.txt
```

또는 Mac/Linux에서는:

```bash
cat result/metrics.txt
```

### 들어갈 이유

최종 성능 결과를 가장 직접적으로 보여주는 파일입니다.

포함해야 할 내용:

* Train F1
* Test F1
* Overfit gap
* Precision
* Recall
* F1-score
* 클래스별 리포트

### 추천 표시

아래 항목에 박스를 넣습니다.

* `Overfit gap 0.0005`
* `공격(1) Recall 0.9965`
* `Test F1 0.9979`

---

## 캡처 ② 혼동행렬

### 종류

🖼️ 결과 이미지

### 정확한 위치

`result/confusion_matrix.png`

### 들어갈 이유

정상/공격 분류가 얼마나 잘 되었는지 시각적으로 보여줄 수 있습니다.

### 추천 표시

대각선 영역, 즉 정답 예측 부분을 강조합니다.

---

## 캡처 ③ 평가 코드

### 종류

📝 코드 캡처

### 정확한 위치

`modules/evaluate.py` 98~119줄

### 들어갈 이유

Accuracy만 본 것이 아니라 Precision, Recall, F1-score를 함께 계산했다는 점을 보여줄 수 있습니다.

### 추천 표시

아래 함수에 강조 표시합니다.

* `precision_score`
* `recall_score`
* `f1_score`

---

# 슬라이드 12. Feature Importance

## Before / After 비교

---

# ⭐ 핵심 슬라이드

---

## 캡처 ① Before — 누수 제거 전 중요도

### 종류

🖼️ 결과 이미지

### 정확한 위치

루트 폴더의:

`feature_importance_kaggle.png`

### 들어갈 이유

누수 제거 전에는 모델이 `Source IP` 같은 식별자에 의존했다는 증거입니다.

### 추천 표시

최상단 `Source IP` 막대에 빨간 박스를 넣고
옆에 **“IP 암기!”** 라고 주석을 추가합니다.

---

## 캡처 ② After — 누수 제거 후 중요도

### 종류

🖼️ 결과 이미지

### 정확한 위치

`result/feature_importance.png`

### 들어갈 이유

누수 제거 후 모델이 IP가 아니라 실제 행동 특성 중심으로 판단하게 되었음을 보여줄 수 있습니다.

### 추천 표시

상위 막대에 초록 박스를 넣고
옆에 **“행동 특성 학습!”** 이라고 주석을 추가합니다.

---

## 캡처 ③ 중요도 추출 코드

### 종류

📝 코드 캡처

### 정확한 위치

`modules/evaluate.py` 136~147줄

### 들어갈 이유

Feature Importance 그림이 코드에서 어떻게 생성되었는지 보여줄 수 있습니다.

### 추천 표시

아래 코드에 강조 표시합니다.

`model.feature_importances_`

---

# 슬라이드 13. 튜닝 한계

## 튜닝 효과는 작았지만, 의미 있는 발견이었다

---

## 캡처 ① 탐색 파라미터 정의

### 종류

📝 코드 캡처

### 정확한 위치

`modules/tune.py` 19~27줄
`param_grid`

### 들어갈 이유

튜닝을 대충 한 것이 아니라, 여러 하이퍼파라미터를 실제로 탐색했다는 증거입니다.

### 추천 표시

아래 항목에 강조 표시합니다.

* `max_depth`
* `max_features`
* `min_samples_leaf`
* `n_estimators`

---

## 캡처 ② 튜닝 결과

### 종류

💻 터미널 출력 캡처

### 실행 방법

```bash
python experiments/extreme_tuning.py
```

### 캡처할 로그 예시

```text
[기본값 Test F1] 0.9979
[튜닝 후 Test F1] 0.9980
[차이] 0.0001
[최악 조합] n_estimators=10, max_depth=2 / CV F1: 0.9632
```

### 들어갈 이유

튜닝을 소홀히 한 것이 아니라, 광범위한 탐색을 통해 기본값이 이미 충분히 좋은 선택임을 확인했다는 근거입니다.

### 추천 표시

`[차이] 0.0001`에 박스를 넣습니다.

---

# 슬라이드 14. 개선 방향·결론

## 재현성과 제출 산출물

---

## 캡처 ① random_state=42 통일

### 종류

📝 코드 캡처

### 정확한 위치

다음 파일의 `RANDOM_STATE = 42` 부분:

* `modules/train.py`
* `modules/evaluate.py`
* `modules/tune.py`

### 들어갈 이유

누가 실행해도 같은 결과가 나오도록 재현성을 확보했다는 점을 보여줄 수 있습니다.

### 추천 표시

`42`에 동일한 강조색을 넣습니다.

---

## 캡처 ② 모델 저장 로그

### 종류

💻 터미널 출력 캡처

### 실행 방법

```bash
python main.py
```

### 캡처할 로그

```text
모델 저장 완료: final_model.pkl
```

### 들어갈 이유

최종 제출 산출물인 `final_model.pkl`이 정상 생성되었음을 보여줄 수 있습니다.

### 추천 표시

`final_model.pkl`에 박스를 넣습니다.

---

# 2. 캡처 우선순위 표

| 슬라이드 | 캡처               | 종류  | 위치                             | 우선순위 |
| ---- | ---------------- | --- | ------------------------------ | ---- |
| 6    | 식별자 제거 함수        | 📝  | preprocessing.py 38~48 + 27~35 | ★필수  |
| 6    | fit/transform 분리 | 📝  | preprocessing.py 108~111       | ★필수  |
| 12   | Before 중요도       | 🖼️ | feature_importance_kaggle.png  | ★필수  |
| 12   | After 중요도        | 🖼️ | result/feature_importance.png  | ★필수  |
| 11   | metrics.txt      | 🖼️ | result/metrics.txt             | ★필수  |
| 11   | 혼동행렬 After       | 🖼️ | result/confusion_matrix.png    | ★필수  |
| 9    | 모델 비교 로그         | 💻  | main.py 실행 출력                  | ★필수  |
| 3    | 크기·클래스 분포        | 💻  | value_counts                   | ★필수  |
| 6    | 누수 방지 로그         | 💻  | main.py 실행 출력                  | 권장   |
| 7    | 모델 후보 정의         | 📝  | evaluate.py 39~55              | 권장   |
| 10   | RF 선택 docstring  | 📝  | train.py 4~13                  | 권장   |
| 13   | 튜닝 차이 0.0001     | 💻  | extreme_tuning.py 출력           | 권장   |
| 13   | param_grid       | 📝  | tune.py 19~27                  | 권장   |
| 5    | 컬럼·결측 요약         | 💻  | describe / columns             | 권장   |
| 8    | main() 흐름        | 📝  | main.py 26~끝                   | 권장   |
| 11   | 지표 계산 코드         | 📝  | evaluate.py 98~119             | 선택   |
| 12   | 중요도 추출 코드        | 📝  | evaluate.py 136~147            | 선택   |
| 10   | 학습·저장 함수         | 📝  | train.py 22~38                 | 선택   |
| 8    | 설정 변수            | 📝  | main.py 16~23                  | 선택   |
| 14   | random_state=42  | 📝  | train / evaluate / tune        | 선택   |
| 14   | 모델 저장 로그         | 💻  | main.py 출력                     | 선택   |
| 2    | main docstring   | 📝  | main.py 1~9                    | 선택   |
| 4    | ID_COLUMNS       | 📝  | preprocessing.py 27~35         | 선택   |
| 5    | 특성 수 로그          | 💻  | main.py 출력                     | 선택   |
| 3    | 분포 막대            | 🖼️ | 직접 생성                          | 선택   |

---

# 3. 캡처 실행 요약

## 1단계. 데이터 배치

아래 위치에 데이터 파일을 둡니다.

```text
data/ddos_dataset.csv
```

---

## 2단계. main.py 실행

```bash
python main.py
```

이 실행으로 아래 슬라이드에 필요한 출력과 결과물이 생성됩니다.

* 슬라이드 3
* 슬라이드 5
* 슬라이드 6
* 슬라이드 9
* 슬라이드 11
* 슬라이드 14

또한 `result/` 폴더가 갱신됩니다.

---

## 3단계. 튜닝 실험 실행

```bash
python experiments/extreme_tuning.py
```

이 실행으로 슬라이드 13에 필요한 튜닝 결과를 얻습니다.

---

## 4단계. 이미지 파일 삽입

아래 파일들은 PPT에 그대로 삽입합니다.

```text
result/confusion_matrix.png
result/feature_importance.png
feature_importance_kaggle.png
```

---

## 5단계. 코드 캡처

VS Code에서 위에 적힌 줄 번호로 이동한 뒤 캡처합니다.

코드 캡처 시에는 다음 원칙을 지킵니다.

* 한 화면에 너무 많은 코드를 넣지 않기
* 핵심 줄에 박스 또는 밑줄 표시하기
* 발표에서 설명할 코드만 캡처하기
* 글씨가 작으면 VS Code 확대 후 캡처하기

---

# 4. 발표에서 꼭 강조할 핵심 문장

## 핵심 문장 1

이 프로젝트는 단순히 높은 정확도를 얻는 것이 아니라,
**데이터 누수를 제거한 뒤에도 모델이 실제 트래픽 행동 특성으로 DDoS를 탐지할 수 있는지 검증한 프로젝트입니다.**

---

## 핵심 문장 2

IP와 Port 같은 식별자 컬럼은 성능을 높여 보이게 만들 수 있지만,
실제 환경에서는 일반화 성능을 해칠 수 있기 때문에 제거했습니다.

---

## 핵심 문장 3

Random Forest는 여러 결정 트리를 결합하는 앙상블 모델이기 때문에,
단일 결정 트리보다 안정적이고 과적합에 강한 예측을 만들 수 있습니다.

---

## 핵심 문장 4

튜닝으로 성능이 크게 오르지 않았다는 점은 실패가 아니라,
현재 데이터가 이미 모델이 학습 가능한 패턴을 충분히 제공하고 있다는 분석 결과입니다.

---

## 핵심 문장 5

최종적으로 중요한 것은 Test F1 0.9979라는 수치뿐만 아니라,
왜 그 성능이 나왔고 어떤 feature를 근거로 판단했는지 설명할 수 있다는 점입니다.
